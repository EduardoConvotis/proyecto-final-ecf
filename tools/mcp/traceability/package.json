{
  "name": "@fieldops/mcp-traceability",
  "version": "0.1.0",
  "private": true,
  "description": "MCP server that serves the SDD traceability graph (constitution laws, requirements, coverage gaps).",
  "type": "module",
  "bin": {
    "mcp-traceability": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "test": "node --import tsx --test test/parse.test.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
